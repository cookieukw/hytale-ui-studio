import type { HytaleComponent } from "./hytale-types";

export const COMMON_UI_DEFINITIONS: Record<string, Partial<HytaleComponent>> = {
  // Add common UI definitions here if needed
  "Button Default": {
    // example properties
  },
};

export const COMMON_UI_KEYS = Object.keys(COMMON_UI_DEFINITIONS);
