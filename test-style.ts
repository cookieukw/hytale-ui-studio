import { parseAndMapCode } from "./lib/hytale-parser";
import { getComponentStyle } from "./components/editor/rendered-component"; // wait, it's a huge React component, I can't just import getComponentStyle if it's inside the component or hooks
